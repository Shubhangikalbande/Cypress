/**
 * Path to cypress.
 */
const cypressPath = process.cwd()

/**
 * Path from cypress to audio folder.
 */
const audioPath =  '\/cypress\/audio\/'

module.exports = {
    cypressPath: cypressPath,
    audioPath: audioPath,
}